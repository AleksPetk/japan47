# Production API used by default. Override both values when testing against a
# Django server on the same Wi-Fi, using the computer's LAN IP (not localhost).
EXPO_PUBLIC_API_URL=https://japan47.alekspetk.com/api/v1
EXPO_PUBLIC_SITE_URL=https://japan47.alekspetk.com
